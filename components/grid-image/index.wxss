.title {
  font-weight: 500;
  font-size: 12px;
  color: var(--theme-head-line);
}
.describe {
  font-size: 10px;
  color: var(--theme-paragraph);
}
.like {
  font-size: 10px;
  color: var(--theme-paragraph);
}
