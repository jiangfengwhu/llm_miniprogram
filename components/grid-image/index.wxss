.title {
  font-weight: 500;
  font-size: 12px;
  color: var(--theme-head-line);
}
.describe {
  font-size: 10px;
  color: var(--theme-paragraph);
}
.like {
  font-size: 10px;
  color: var(--theme-paragraph);
}
.no-image {
  width: 92%;
  height: 150px;
  border-radius: 8px;
  background-color: #ccc;
  display: flex;
  justify-content: center;
  align-items: center;
}
